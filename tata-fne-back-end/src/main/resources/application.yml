spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
  application:
    name: ModernHygienFneBackend
springdoc:
  default-produces-media-type: application/json
server:
  forward-headers-strategy: framework
  servlet:
    context-path: /api/v1/
